spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/computer_shop
    username: postgres
    password: Pa$$w0rd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect